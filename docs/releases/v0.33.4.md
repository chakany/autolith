# Autolith 0.33.4

Autolith 0.33.4 lets recovery-image builds run in a Git checkout owned by another user.

- Pass `safe.directory=*` to recovery provenance Git commands so root CI on a runner-owned tree can read the source identity.

[Full commit history](https://github.com/lambda-symbolics/autolith/compare/v0.33.3...v0.33.4)
