# Autolith 0.33.5

Autolith 0.33.5 lets active-image and archive builds run in a Git checkout owned by another user.

- Pass `safe.directory=*` to active-image and release-archive Git commands.

[Full commit history](https://github.com/lambda-symbolics/autolith/compare/v0.33.4...v0.33.5)
