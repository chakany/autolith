# Autolith 0.26.1

Autolith 0.26.1 supersedes 0.26.0 before Linux binary publication and completes its release verification.

- Guarantee that the active-tool cancellation test always stops its application and reports producer-thread failures instead of leaving repository checks blocked indefinitely.
- Render localgroup table column junctions with `┬` beneath the unpartitioned title, `┼` between rows, and `┴` along the bottom border.

[Full commit history](https://github.com/luciusmagn/autolith/compare/v0.26.0...v0.26.1)
